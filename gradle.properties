org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
android.suppressUnsupportedCompileSdk=35

# Release signing properties are intentionally NOT defined here.
# See gradle.properties.example for the variable names. Define them in your
# user-level ~/.gradle/gradle.properties (Linux/macOS) or
# %USERPROFILE%\.gradle\gradle.properties (Windows) to sign release builds.
# If unset, release builds fall back to the debug signing config (see app/build.gradle.kts).
